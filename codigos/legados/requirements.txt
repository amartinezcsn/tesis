# Dependencias portables del proyecto
# Python compatible: 3.10 a 3.13 (64 bits recomendado).
# pip selecciona la version compatible con el sistema operativo y Python.

numpy>=1.24,<3
pandas>=2.0,<4
scipy>=1.10,<2
scikit-learn>=1.3,<2
statsmodels>=0.14,<1
matplotlib>=3.7,<4
openpyxl>=3.1,<4
joblib>=1.3,<2

# Solo se importa durante la etapa opcional RNN/LSTM.
# No se instala en Python 3.14+ hasta que exista soporte estable.
tensorflow>=2.13,<3; python_version < "3.14"
